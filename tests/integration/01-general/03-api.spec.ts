/// <reference types="Cypress" />

// Working variables
const url = Cypress.env("URL");

// API
describe("API", () => {
  // I. TEST: Count
  it("Count", () => {});

  // II. TEST: Data order
  it("Order", () => {});

  // III. TEST: Maps all data to props
  it("Maps to element correctly", () => {});
});
